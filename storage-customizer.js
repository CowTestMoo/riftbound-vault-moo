(() => {
  'use strict';
  const APP_KEY='riftbound-vault-v2';
  const DOMAINS=['Fury','Calm','Mind','Body','Chaos','Order'];
  const RULES=[['All','All regular domain cards'],['Units','Units'],['Other','Other standard cards'],['Champions','Champions'],['Legends','Legends'],['LegendsChampions','Legends + Champion Units'],['Spells','Spells'],['Gear','Gear'],['Runes','Runes'],['Battlefields','Battlefields / Maps'],['Tokens','Tokens'],['TokensMaps','Tokens + Battlefields / Maps'],['NoRune','No Rune / Colorless Cards']];
  let draft=[],dragId='';
  const esc=(v='')=>String(v).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
  const uid=()=>`box-${Date.now().toString(36)}-${Math.random().toString(36).slice(2,8)}`;
  const readState=()=>{try{return JSON.parse(localStorage.getItem(APP_KEY)||'{}')}catch{return {}}};

  function defaults(){
    if(window.RiftboundSpecialCards?.recommendedStorageBoxes)return window.RiftboundSpecialCards.recommendedStorageBoxes();
    return [
      {id:'box-legends-champions',name:'Legends + Champion Units',domains:[],rule:'LegendsChampions'},
      {id:'box-tokens-maps',name:'Tokens + Battlefields / Maps',domains:[],rule:'TokensMaps'},
      {id:'box-no-rune',name:'No Rune / Colorless Cards',domains:[],rule:'NoRune'},
      ...DOMAINS.map(d=>({id:`box-${d.toLowerCase()}`,name:`${d} Cards`,domains:[d],rule:'All'}))
    ];
  }

  function normalize(boxes){
    if(!Array.isArray(boxes))return defaults();
    return boxes.map((b,i)=>({id:String(b?.id||uid()),name:String(b?.name||`Box ${i+1}`),domains:Array.isArray(b?.domains)?b.domains.filter(d=>DOMAINS.includes(d)):[],rule:RULES.some(([v])=>v===b?.rule)?b.rule:'All'}));
  }

  function cardClass(card){
    if(window.RiftboundSpecialCards?.storageClass)return window.RiftboundSpecialCards.storageClass(card);
    const labels=(card?.cardTypeLabels||[]).map(x=>String(x).toLowerCase()),champ=labels.includes('champion'),type=String(card?.cardType||'').toLowerCase();
    return type==='legend'?'Legends':champ?'Champions':type==='unit'?'Units':type==='spell'?'Spells':type==='gear'||type==='item'?'Gear':type==='rune'?'Runes':type==='battlefield'?'Battlefields':type==='token'?'Tokens':'Other';
  }
  function cardDomains(card){const ds=Array.isArray(card?.domains)?card.domains:(card?.domain?[card.domain]:[]);return [...new Set(ds.filter(d=>DOMAINS.includes(d)))]}
  function domainMatches(box,card){if(!box.domains.length)return true;const domains=cardDomains(card);return box.domains.some(domain=>domains.includes(domain))}
  function isNoRuneRegular(card){if(window.RiftboundSpecialCards?.isNoRuneRegular)return window.RiftboundSpecialCards.isNoRuneRegular(card);const cls=cardClass(card);return cardDomains(card).length===0&&!['Legends','Champions','Battlefields','Tokens'].includes(cls)}
  function matches(box,card){const cls=cardClass(card);if(box.rule==='NoRune')return isNoRuneRegular(card);if(!domainMatches(box,card))return false;if(box.rule==='LegendsChampions')return cls==='Legends'||cls==='Champions';if(box.rule==='TokensMaps')return cls==='Tokens'||cls==='Battlefields';if(box.rule==='All')return !['Legends','Champions','Battlefields','Tokens'].includes(cls)&&cardDomains(card).length>0;if(box.rule==='Other')return !['Units','Legends','Champions','Battlefields','Tokens'].includes(cls);return box.rule===cls}
  function findBoxIndex(card){const cls=cardClass(card);if(cls==='Legends'||cls==='Champions'){const combined=draft.findIndex(box=>box.rule==='LegendsChampions'&&domainMatches(box,card));if(combined>=0)return combined;const exact=draft.findIndex(box=>box.rule===cls&&domainMatches(box,card));if(exact>=0)return exact}if(cls==='Battlefields'||cls==='Tokens'){const combined=draft.findIndex(box=>box.rule==='TokensMaps'&&domainMatches(box,card));if(combined>=0)return combined;const exact=draft.findIndex(box=>box.rule===cls&&domainMatches(box,card));if(exact>=0)return exact}if(isNoRuneRegular(card)){const nr=draft.findIndex(box=>box.rule==='NoRune');if(nr>=0)return nr}return draft.findIndex(box=>matches(box,card))}
  function ruleLabel(v){return RULES.find(([x])=>x===v)?.[1]||v}
  function domainSummary(b){return b.domains.length?b.domains.join(' + '):'Any domain'}
  function nextName(){const used=new Set(draft.map(b=>b.name));let n=draft.length+1;while(used.has(`Box ${n}`))n++;return `Box ${n}`}

  function ensureButton(){const view=document.getElementById('storageView'),heading=view?.querySelector('.section-heading');if(!heading||document.getElementById('customizeStorageBtn'))return;heading.classList.add('inline-heading','storage-heading-customizable');let copy=heading.querySelector(':scope > div');if(!copy){copy=document.createElement('div');const h=heading.querySelector('h2'),p=heading.querySelector('p');if(h)copy.appendChild(h);if(p)copy.appendChild(p);heading.prepend(copy)}const b=document.createElement('button');b.id='customizeStorageBtn';b.className='ghost-btn';b.type='button';b.textContent='Customize Storage';heading.appendChild(b)}

  function ensureDialog(){if(document.getElementById('storageSetupDialog'))return;const d=document.createElement('dialog');d.id='storageSetupDialog';d.className='modal storage-setup-dialog';d.innerHTML=`<div class="modal-inner storage-setup-inner"><div class="modal-head"><div><h2>Customize Storage</h2><p class="storage-setup-subtitle">Build the website in the same order as your real storage.</p></div><button class="close-btn" data-close-storage-setup>×</button></div><div class="storage-setup-help"><strong>Recommended layout:</strong> combine Legends + Champion Units, combine Tokens + Battlefields / Maps, keep cards with no rune/domain together in No Rune / Colorless Cards, then use one section for each of the six domains. Inside a domain, sort Units by Energy, then Spells, Gear, Runes, and Other cards.</div><div id="storageSetupSummary" class="storage-setup-summary"></div><div id="storageSetupList" class="storage-setup-list"></div><div class="storage-setup-footer"><div><button id="addStorageBox" class="ghost-btn" type="button">+ Add Section</button><button id="resetStorageBoxes" class="ghost-btn" type="button">Reset to Recommended Layout</button></div><button id="saveStorageBoxes" class="primary-btn" type="button">Save Storage Layout</button></div></div>`;document.body.appendChild(d)}

  function preview(){const catalog=window.RiftboundApp?.getCatalog?.()||[],s=readState(),counts=new Map(draft.map(b=>[b.id,{copies:0,unique:0}]));let uc=0,uu=0;for(const c of catalog){const owned=Number(s.inventory?.[c.cardCode]?.owned||0);if(owned<=0)continue;const i=findBoxIndex(c);if(i<0){uc+=owned;uu++;continue}const x=counts.get(draft[i].id);x.copies+=owned;x.unique++}return {counts,uc,uu}}
  function render(){const root=document.getElementById('storageSetupList');if(!root)return;const p=preview(),sum=document.getElementById('storageSetupSummary');if(sum)sum.innerHTML=`<div><strong>${draft.length}</strong><small>Storage sections</small></div><div class="${p.uc?'warn':'good'}"><strong>${p.uc}</strong><small>Unassigned copies</small></div><div class="${p.uu?'warn':'good'}"><strong>${p.uu}</strong><small>Unassigned cards</small></div>`;root.innerHTML=draft.length?draft.map((b,i)=>{const c=p.counts.get(b.id)||{copies:0,unique:0};return `<article class="storage-rule-card" draggable="true" data-storage-box-id="${esc(b.id)}"><div class="storage-rule-order"><span class="storage-drag-handle">⋮⋮</span><strong>${String(i+1).padStart(2,'0')}</strong><div><button data-storage-up="${esc(b.id)}" ${i===0?'disabled':''}>↑</button><button data-storage-down="${esc(b.id)}" ${i===draft.length-1?'disabled':''}>↓</button></div></div><div class="storage-rule-main"><label class="storage-name-label"><span>Section / box name</span><input data-storage-name="${esc(b.id)}" value="${esc(b.name)}"></label><div class="storage-rule-meta"><span>${c.copies} copies</span><span>${c.unique} unique</span></div><div class="storage-rule-fields"><div><span class="field-label">Domains <small>none = any</small></span><div class="storage-domain-chips">${DOMAINS.map(d=>`<label class="storage-domain-chip ${b.domains.includes(d)?'active':''}"><input type="checkbox" data-storage-domain="${d}" ${b.domains.includes(d)?'checked':''}><span>${d}</span></label>`).join('')}</div></div><label><span class="field-label">What goes here</span><select data-storage-rule="${esc(b.id)}">${RULES.map(([v,l])=>`<option value="${v}" ${b.rule===v?'selected':''}>${l}</option>`).join('')}</select></label></div><div class="storage-rule-preview">${esc(domainSummary(b))} • ${esc(ruleLabel(b.rule))}</div></div><button class="storage-remove-btn" data-storage-remove="${esc(b.id)}">×</button></article>`}).join(''):'<div class="empty-state">You have no storage sections configured. Add one below.</div>'}

  function open(){draft=normalize(readState().storageBoxes).map(b=>({...b,domains:[...b.domains]}));render();document.getElementById('storageSetupDialog').showModal()}
  function move(id,delta){const i=draft.findIndex(b=>b.id===id),j=i+delta;if(i<0||j<0||j>=draft.length)return;[draft[i],draft[j]]=[draft[j],draft[i]];render()}
  function save(){const s=readState();s.storageBoxes=normalize(draft);s.storageLayoutVersion=5;s.transactions=Array.isArray(s.transactions)?s.transactions:[];s.transactions.unshift({id:`evt-${Date.now().toString(36)}`,type:'activity',action:`Updated storage layout • ${s.storageBoxes.length} section${s.storageBoxes.length===1?'':'s'}`,at:new Date().toISOString()});s.transactions=s.transactions.slice(0,500);localStorage.setItem(APP_KEY,JSON.stringify(s));window.RiftboundApp?.reloadState?.();window.RiftboundCloud?.syncNow?.();document.getElementById('storageSetupDialog').close()}

  function bind(){document.addEventListener('click',e=>{let x;if(e.target.closest('#customizeStorageBtn'))return open();if(e.target.closest('[data-close-storage-setup]'))return document.getElementById('storageSetupDialog')?.close();if(e.target.closest('#addStorageBox')){draft.push({id:uid(),name:nextName(),domains:[],rule:'All'});return render()}if(e.target.closest('#resetStorageBoxes')){if(confirm('Reset your storage layout to the recommended 9-section setup?')){draft=defaults();render()}return}if(e.target.closest('#saveStorageBoxes'))return save();if(x=e.target.closest('[data-storage-remove]')){draft=draft.filter(b=>b.id!==x.dataset.storageRemove);return render()}if(x=e.target.closest('[data-storage-up]'))return move(x.dataset.storageUp,-1);if(x=e.target.closest('[data-storage-down]'))return move(x.dataset.storageDown,1)});document.addEventListener('input',e=>{if(e.target.matches('[data-storage-name]')){const b=draft.find(x=>x.id===e.target.dataset.storageName);if(b)b.name=e.target.value}});document.addEventListener('change',e=>{if(e.target.matches('[data-storage-rule]')){const b=draft.find(x=>x.id===e.target.dataset.storageRule);if(b)b.rule=e.target.value;return render()}if(e.target.matches('[data-storage-domain]')){const id=e.target.closest('[data-storage-box-id]')?.dataset.storageBoxId,b=draft.find(x=>x.id===id);if(!b)return;const d=e.target.dataset.storageDomain;b.domains=e.target.checked?[...new Set([...b.domains,d])]:b.domains.filter(x=>x!==d);return render()}});document.addEventListener('dragstart',e=>{const c=e.target.closest('[data-storage-box-id]');if(!c)return;dragId=c.dataset.storageBoxId;c.classList.add('dragging')});document.addEventListener('dragend',e=>{e.target.closest?.('[data-storage-box-id]')?.classList.remove('dragging');dragId=''});document.addEventListener('dragover',e=>{if(e.target.closest?.('[data-storage-box-id]')&&dragId)e.preventDefault()});document.addEventListener('drop',e=>{const t=e.target.closest?.('[data-storage-box-id]');if(!t||!dragId||t.dataset.storageBoxId===dragId)return;e.preventDefault();const from=draft.findIndex(b=>b.id===dragId),to=draft.findIndex(b=>b.id===t.dataset.storageBoxId);if(from<0||to<0)return;const [m]=draft.splice(from,1);draft.splice(to,0,m);dragId='';render()})}

  function init(){const s=readState();if(!Array.isArray(s.storageBoxes)){s.storageBoxes=defaults();s.storageLayoutVersion=5;localStorage.setItem(APP_KEY,JSON.stringify(s))}ensureButton();ensureDialog();bind()}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();
})();
