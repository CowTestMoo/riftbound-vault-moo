(() => {
  'use strict';
  const APP_KEY='riftbound-vault-v2';
  const DOMAINS=['Fury','Calm','Mind','Body','Chaos','Order'];
  const RULES=[['All','All cards'],['Units','Units'],['Other','Non-unit cards'],['Champions','Champions'],['Spells','Spells'],['Gear','Gear'],['Runes','Runes'],['Battlefields','Battlefields'],['Tokens','Tokens']];
  let draft=[],dragId='';
  const esc=(v='')=>String(v).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
  const uid=()=>`box-${Date.now().toString(36)}-${Math.random().toString(36).slice(2,8)}`;
  const readState=()=>{try{return JSON.parse(localStorage.getItem(APP_KEY)||'{}')}catch{return {}}};
  function defaults(){return DOMAINS.flatMap((d,i)=>[{id:`box-${i*2+1}`,name:`Box ${i*2+1}`,domains:[d],rule:'Units'},{id:`box-${i*2+2}`,name:`Box ${i*2+2}`,domains:[d],rule:'Other'}])}
  function normalize(boxes){if(!Array.isArray(boxes))return defaults();return boxes.map((b,i)=>({id:String(b?.id||uid()),name:String(b?.name||`Box ${i+1}`),domains:Array.isArray(b?.domains)?b.domains.filter(d=>DOMAINS.includes(d)):[],rule:RULES.some(([v])=>v===b?.rule)?b.rule:'All'}))}
  function cardClass(card){const labels=(card?.cardTypeLabels||[]).map(x=>String(x).toLowerCase()),champ=labels.includes('champion'),type=String(card?.cardType||'').toLowerCase();return type==='unit'&&!champ?'Units':champ?'Champions':type==='spell'?'Spells':type==='gear'?'Gear':type==='rune'?'Runes':type==='battlefield'?'Battlefields':type==='token'?'Tokens':'Other'}
  function cardDomain(card){const ds=Array.isArray(card?.domains)?card.domains:(card?.domain?[card.domain]:[]);return DOMAINS.find(d=>ds.includes(d))||(DOMAINS.includes(card?.domain)?card.domain:'Unassigned')}
  function matches(box,card){if(box.domains.length&&!box.domains.includes(cardDomain(card)))return false;const cls=cardClass(card);if(box.rule==='All')return true;if(box.rule==='Other')return cls!=='Units';return box.rule===cls}
  function ruleLabel(v){return RULES.find(([x])=>x===v)?.[1]||v}
  function domainSummary(b){return b.domains.length?b.domains.join(' + '):'Any domain'}
  function nextName(){const used=new Set(draft.map(b=>b.name));let n=draft.length+1;while(used.has(`Box ${n}`))n++;return `Box ${n}`}
  function ensureButton(){const view=document.getElementById('storageView'),heading=view?.querySelector('.section-heading');if(!heading||document.getElementById('customizeStorageBtn'))return;heading.classList.add('inline-heading','storage-heading-customizable');let copy=heading.querySelector(':scope > div');if(!copy){copy=document.createElement('div');const h=heading.querySelector('h2'),p=heading.querySelector('p');if(h)copy.appendChild(h);if(p)copy.appendChild(p);heading.prepend(copy)}copy.querySelector('h2').textContent='Your Storage';copy.querySelector('p').textContent='Mirror your real storage boxes and choose where each kind of card belongs.';const b=document.createElement('button');b.id='customizeStorageBtn';b.className='primary-btn';b.type='button';b.textContent='Customize Storage';heading.appendChild(b)}
  function ensureDialog(){if(document.getElementById('storageSetupDialog'))return;const d=document.createElement('dialog');d.id='storageSetupDialog';d.className='modal storage-setup-dialog';d.innerHTML=`<div class="modal-inner storage-setup-inner"><div class="modal-head"><div><h2>Customize Storage</h2><p class="storage-setup-subtitle">Build the website in the same order as your real boxes.</p></div><button class="close-btn" data-close-storage-setup>×</button></div><div class="storage-setup-help"><strong>Order matters.</strong> A card goes to the first box whose domain and card-type rules match it. Drag boxes or use the arrows to reorder them.</div><div id="storageSetupSummary" class="storage-setup-summary"></div><div id="storageSetupList" class="storage-setup-list"></div><div class="storage-setup-footer"><div><button id="addStorageBox" class="ghost-btn" type="button">+ Add Box</button><button id="resetStorageBoxes" class="ghost-btn" type="button">Reset to 12-Box Default</button></div><button id="saveStorageBoxes" class="primary-btn" type="button">Save Storage Layout</button></div></div>`;document.body.appendChild(d)}
  function preview(){const catalog=window.RiftboundApp?.getCatalog?.()||[],s=readState(),counts=new Map(draft.map(b=>[b.id,{copies:0,unique:0}]));let uc=0,uu=0;for(const c of catalog){const owned=Number(s.inventory?.[c.cardCode]?.owned||0);if(owned<=0)continue;const i=draft.findIndex(b=>matches(b,c));if(i<0){uc+=owned;uu++;continue}const x=counts.get(draft[i].id);x.copies+=owned;x.unique++}return {counts,uc,uu}}
  function render(){const root=document.getElementById('storageSetupList');if(!root)return;const p=preview(),sum=document.getElementById('storageSetupSummary');if(sum)sum.innerHTML=`<div><strong>${draft.length}</strong><small>Boxes</small></div><div class="${p.uc?'warn':'good'}"><strong>${p.uc}</strong><small>Unassigned copies</small></div><div class="${p.uu?'warn':'good'}"><strong>${p.uu}</strong><small>Unassigned cards</small></div>`;root.innerHTML=draft.length?draft.map((b,i)=>{const c=p.counts.get(b.id)||{copies:0,unique:0};return `<article class="storage-rule-card" draggable="true" data-storage-box-id="${esc(b.id)}"><div class="storage-rule-order"><span class="storage-drag-handle">⋮⋮</span><strong>${String(i+1).padStart(2,'0')}</strong><div><button data-storage-up="${esc(b.id)}" ${i===0?'disabled':''}>↑</button><button data-storage-down="${esc(b.id)}" ${i===draft.length-1?'disabled':''}>↓</button></div></div><div class="storage-rule-main"><label class="storage-name-label"><span>Box name</span><input data-storage-name="${esc(b.id)}" value="${esc(b.name)}"></label><div class="storage-rule-meta"><span>${c.copies} copies</span><span>${c.unique} unique</span></div><div class="storage-rule-fields"><div><span class="field-label">Domains <small>none = any</small></span><div class="storage-domain-chips">${DOMAINS.map(d=>`<label class="storage-domain-chip ${b.domains.includes(d)?'active':''}"><input type="checkbox" data-storage-domain="${d}" ${b.domains.includes(d)?'checked':''}><span>${d}</span></label>`).join('')}</div></div><label><span class="field-label">What goes here</span><select data-storage-rule="${esc(b.id)}">${RULES.map(([v,l])=>`<option value="${v}" ${b.rule===v?'selected':''}>${l}</option>`).join('')}</select></label></div><div class="storage-rule-preview">${esc(domainSummary(b))} • ${esc(ruleLabel(b.rule))}</div></div><button class="storage-remove-btn" data-storage-remove="${esc(b.id)}">×</button></article>`}).join(''):'<div class="empty-state">You have no storage boxes configured. Add one below.</div>'}
  function open(){draft=normalize(readState().storageBoxes).map(b=>({...b,domains:[...b.domains]}));render();document.getElementById('storageSetupDialog').showModal()}
  function move(id,delta){const i=draft.findIndex(b=>b.id===id),j=i+delta;if(i<0||j<0||j>=draft.length)return;[draft[i],draft[j]]=[draft[j],draft[i]];render()}
  function save(){const s=readState();s.storageBoxes=normalize(draft);s.transactions=Array.isArray(s.transactions)?s.transactions:[];s.transactions.unshift({id:`evt-${Date.now().toString(36)}`,type:'activity',action:`Updated storage layout • ${s.storageBoxes.length} box${s.storageBoxes.length===1?'':'es'}`,at:new Date().toISOString()});s.transactions=s.transactions.slice(0,500);localStorage.setItem(APP_KEY,JSON.stringify(s));window.RiftboundApp?.reloadState?.();window.RiftboundCloud?.syncNow?.();document.getElementById('storageSetupDialog').close()}
  function bind(){document.addEventListener('click',e=>{let x;if(e.target.closest('#customizeStorageBtn'))return open();if(e.target.closest('[data-close-storage-setup]'))return document.getElementById('storageSetupDialog')?.close();if(e.target.closest('#addStorageBox')){draft.push({id:uid(),name:nextName(),domains:['Fury'],rule:'All'});return render()}if(e.target.closest('#resetStorageBoxes')){if(confirm('Reset your storage layout to the original 12-box setup?')){draft=defaults();render()}return}if(e.target.closest('#saveStorageBoxes'))return save();if(x=e.target.closest('[data-storage-remove]')){draft=draft.filter(b=>b.id!==x.dataset.storageRemove);return render()}if(x=e.target.closest('[data-storage-up]'))return move(x.dataset.storageUp,-1);if(x=e.target.closest('[data-storage-down]'))return move(x.dataset.storageDown,1)});document.addEventListener('input',e=>{if(e.target.matches('[data-storage-name]')){const b=draft.find(x=>x.id===e.target.dataset.storageName);if(b)b.name=e.target.value}});document.addEventListener('change',e=>{if(e.target.matches('[data-storage-rule]')){const b=draft.find(x=>x.id===e.target.dataset.storageRule);if(b)b.rule=e.target.value;return render()}if(e.target.matches('[data-storage-domain]')){const id=e.target.closest('[data-storage-box-id]')?.dataset.storageBoxId,b=draft.find(x=>x.id===id);if(!b)return;const d=e.target.dataset.storageDomain;b.domains=e.target.checked?[...new Set([...b.domains,d])]:b.domains.filter(x=>x!==d);return render()}});document.addEventListener('dragstart',e=>{const c=e.target.closest('[data-storage-box-id]');if(!c)return;dragId=c.dataset.storageBoxId;c.classList.add('dragging')});document.addEventListener('dragend',e=>{e.target.closest?.('[data-storage-box-id]')?.classList.remove('dragging');dragId=''});document.addEventListener('dragover',e=>{if(e.target.closest?.('[data-storage-box-id]')&&dragId)e.preventDefault()});document.addEventListener('drop',e=>{const t=e.target.closest?.('[data-storage-box-id]');if(!t||!dragId||t.dataset.storageBoxId===dragId)return;e.preventDefault();const from=draft.findIndex(b=>b.id===dragId),to=draft.findIndex(b=>b.id===t.dataset.storageBoxId);if(from<0||to<0)return;const [m]=draft.splice(from,1);draft.splice(to,0,m);dragId='';render()})}
  function init(){const s=readState();if(!Array.isArray(s.storageBoxes)){s.storageBoxes=defaults();localStorage.setItem(APP_KEY,JSON.stringify(s))}ensureButton();ensureDialog();bind();const o=new MutationObserver(ensureButton);o.observe(document.body,{childList:true,subtree:true})}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();
})();